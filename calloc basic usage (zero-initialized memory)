#include <stdio.h>
#include <stdlib.h>

int main() {
    int n = 5;
    int *arr = (int *)calloc(n, sizeof(int));
    if (arr == NULL) return 1;

    for (int i = 0; i < n; i++) printf("%d ", arr[i]); // all zeros
    printf("\n");

    free(arr);
    return 0;
}

Output:

0 0 0 0 0
